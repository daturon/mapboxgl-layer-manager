# Required: your Mapbox public token (starts with pk.)
# Get one free at https://mapbox.com → Account → Tokens
VITE_MAPBOX_TOKEN=pk.your_token_here

# Optional: base URL for GitHub Pages deployment
# Set this to /your-repo-name/ when deploying to GitHub Pages
# VITE_BASE_URL=/mapboxgl-layer-manager/
